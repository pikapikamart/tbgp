import { fluid } from "@/styled/functions"
import styled from "styled-components"


export const ArticlesWrapper = styled.div`
  margin-top: ${ fluid(40, 3, 48) };
`

export const ArticleList = styled.ul``